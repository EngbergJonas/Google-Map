/**
 * TODO: Add an address that will be displayed on the map
 * TODO: Add these addresses to a ListView
 * TODO: The addresses should be stored in which order you get the shortest route
 *
 * Show a map !
 * Get the coordinates of an address with GeoCoding !
 *
 * Serialized class Route =
 * String name = (route name)
 * LatLng startPoint(coordinates for startpoint)
 * ArrayList<LatLng> waypoints(a list of coordinates for the addresses)
 * Double distance (the route for the shorts route)
 *
 * Store every route in its own object that will be saved in an (ArrayList<Route>)
 * Store the list in the phones internal memory
 *
 */


